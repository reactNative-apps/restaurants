import React from "react";
import { View, Text, StyleSheet } from "react-native";

const Home = props => {
  const {} = props;

  return (
    <View>
      <Text>Home</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default Home;
